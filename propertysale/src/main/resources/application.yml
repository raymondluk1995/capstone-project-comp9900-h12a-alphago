server:
  servlet:
    context-path:
  port: 8070
# datasource
spring:
  datasource:
    username: root
    url: jdbc:mysql://database-1.cw7lwywc9t1a.ap-southeast-2.rds.amazonaws.com/property_sale?serverTimezone=Australia/Sydney&&useUnicode=true&characterEncoding=UTF-8&&useSSL=true
    password: znx5201314
#    password: root
#    url: jdbc:mysql://localhost:3306/property_sale?serverTimezone=Australia/Sydney&&useUnicode=true&characterEncoding=UTF-8&&useSSL=true
    driver-class-name: com.mysql.cj.jdbc.Driver
# redis
  redis:
    password: 123456
    host: 3.24.78.45
    timeout: 5000

  # mail
  mail:
    host: smtp.gmail.com
    default-encoding: utf-8
    username: zxhhaha@gmail.com
    password: znx5201314
    properties:
      mail:
        smtp:
          # 如果是用 SSL 方式，需要配置如下属性,使用qq邮箱的话需要开启
          ssl:
            enable: true
            required: true
          # 邮件接收时间的限制，单位毫秒
          timeout: 10000
          # 连接时间的限制，单位毫秒
          connectiontimeout: 10000
          # 邮件发送时间的限制，单位毫秒
          writetimeout: 10000
          port: 465
  # rabbitmq
  rabbitmq:
    host: 3.24.78.45
    username: admin
    password: admin
    myExchange: alpha.go.sender
  servlet:
    multipart:
      max-file-size: 10MB

# mybatis plus
mybatis-plus:
  mapper-locations:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    cache-enabled: true
  global-config:
    db-config:
      logic-delete-field: flag  # 全局逻辑删除的实体字段名(since 3.3.0,配置后可以忽略不配置步骤2)
      logic-delete-value: 1 # 逻辑已删除值(默认为 1)
      logic-not-delete-value: 0 # 逻辑未删除值(默认为 0)



remote:
  url: http://3.24.78.45:8060/img/

